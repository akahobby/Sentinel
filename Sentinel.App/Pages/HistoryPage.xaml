<Page
    x:Class="Sentinel.App.Pages.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:models="using:Sentinel.Core.Models"
    xmlns:vm="using:Sentinel.App.ViewModels"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <StackPanel Padding="24" Spacing="24">
            <TextBlock Text="History" Style="{StaticResource TitleTextStyle}" />
            <TextBlock Text="Spike events and change timeline from stored data." Style="{StaticResource CaptionTextStyle}" />
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Content="Load" Command="{Binding LoadCommand}" />
                <ComboBox Header="Days" SelectedItem="{Binding DaysBack, Mode=TwoWay}" ItemsSource="{Binding DaysBackOptions}" MinWidth="80" HorizontalAlignment="Left" />
                <ProgressRing IsActive="{Binding IsLoading}" Width="24" Height="24" VerticalAlignment="Center" />
            </StackPanel>
            <TextBlock Text="{Binding StatusText}" Style="{StaticResource BodyTextStyle}" />

            <controls:ListView x:Name="TimelineList" ItemsSource="{Binding Timeline}" SelectionMode="None">
                <controls:ListView.ItemTemplate>
                    <DataTemplate x:DataType="vm:HistoryEntry">
                        <Border Background="{StaticResource CardBackgroundBrush}" CornerRadius="{StaticResource CornerRadiusCard}" Padding="12" Margin="0,0,0,6">
                            <StackPanel Spacing="4">
                                <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" />
                                <TextBlock Text="{x:Bind Detail}" Style="{StaticResource CaptionTextStyle}" TextWrapping="Wrap" />
                                <TextBlock Text="{x:Bind When}" Style="{StaticResource CaptionTextStyle}" Opacity="0.8" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </controls:ListView.ItemTemplate>
            </controls:ListView>
        </StackPanel>
    </ScrollViewer>
</Page>
